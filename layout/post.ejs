  <h4><%- page.title %></h4>

  <%- page.content%>
  <p><%- list_tags(page.tags, {class: 'classtest', show_count: false, style: false}) %> <% if (theme.date) {- full_date(page.date, 'll')} %></p>
  <%- partial('partial/comments') %>
