<div class="row">
  <div class="two columns" style="max-width: 50px">
    <h4 class="mt-2 mode">
      <div onclick=setDarkMode(true) id="darkBtn"><%= theme.switch_dard %></div>
      <div onclick=setDarkMode(false) id="lightBtn" class=hidden><%= theme.switch_light %></div>
      <script >
        if (localStorage.getItem('preferredTheme') == 'dark') {
          setDarkMode(true)
        }
        function setDarkMode(isDark) {
          var darkBtn = document.getElementById('darkBtn')
          var lightBtn = document.getElementById('lightBtn')
          if (isDark) {
            lightBtn.style.display = "block"
            darkBtn.style.display = "none"
            localStorage.setItem('preferredTheme', 'dark');
          } else {
            lightBtn.style.display = "none"
            darkBtn.style.display = "block"
            localStorage.removeItem('preferredTheme');
          }
          document.body.classList.toggle("darkmode");
        }
      </script>
    </h4>
  </div>
  <style>

  a {
    color: inherit;
    text-decoration-color:  #<%= theme.tcolor %>;
    background-size: 100% 200%;
    background-image: linear-gradient(to bottom, rgba(0,0,0, 0.0) 50%, #<%= theme.tcolor %> 50%);
    transition:
      background-position .2s ease-in-out,
      color .2s ease-in-out;
  }

  .nanobar .bar {
    background: #<%= theme.ncolor %>;
  }

  blockquote {
    font-family: 'Inter';
    background-color: rgba(148, 148, 149, 0.08) ;
    border-left: 8px solid #<%= theme.ncolor %>;
    margin: 1.5em 0px;
    padding: 1.1em 20px 1px 20px;
    font-style: italic;
  }
  </style>

  <div class="six columns ml-1" style="max-width: 200px">
    <h4 class="mt-2">
      <%= theme.title %>
    </h4>
  </div>

  <div class="twelve columns">
    <div class="row">
      <div class="nine columns left">
        <a href="<%= config.root %>" class="link">Home</a>
        <% for (name in theme.menu) { %>
          <a href="<%- url_for(theme.menu[name]) %>" class="ml link"><%- name %></a>
        <% } %>
      </div>
    </div>
    <hr style="margin-bottom: 2.6rem">
  </div>
</div>
